
spring:
  datasource:
#    数据源基本配置：数据库url，账号，密码，连接池
    username: root
    url: jdbc:mysql://47.100.210.244:3306/VEStation
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 1602liuqijun
    type: com.alibaba.druid.pool.DruidDataSource
    # 数据库其他配置
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    #   配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: stat,wall,log4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
mybatis-plus:
  global-config:
    db-config:
      logic-delete-value: 0 #删除时设为0
      logic-not-delete-value: 1 #不删除时设为1
  mapper-locations: classpath:mybatis/mapper/*.xml #指定sql映射文件路径
  config-location: classpath:mybatis/mybatis-config.xml #指定mybatis-plus其他具体配置的路径
  type-aliases-package: com.zhiyi.vestation.pojo #实体类文件